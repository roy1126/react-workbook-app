export class User {
  constructor(key, firstName, lastName, email, eid, birthdate) {
    this.key = key;
    this.firstName = firstName;
    this.lastName = lastName;
    this.email = email;
    this.eid = eid;
    this.birthdate = birthdate;
  }
}
